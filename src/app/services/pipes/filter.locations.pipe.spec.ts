import { Filter.LocationsPipe } from './filter.locations.pipe';

describe('Filter.LocationsPipe', () => {
  it('create an instance', () => {
    const pipe = new Filter.LocationsPipe();
    expect(pipe).toBeTruthy();
  });
});
